name: RDP Skyro Runner

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360 # Hard limit for GitHub Runners is 6 hours

    steps:
      - name: Configure Core RDP Settings
        run: |
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -Name "UserAuthentication" -Value 1 -Force
          netsh advfirewall firewall add rule name="RDP-Tailscale" dir=in action=allow protocol=TCP localport=3389
          Restart-Service -Name TermService -Force

      - name: Create RDP User with Secure Password
        run: |
          $charSet = @{ Upper = [char[]](65..90); Lower = [char[]](97..122); Number = [char[]](48..57) }
          $ranijay86#$ = -join (($charSet.Upper | Get-Random -Count 6) + ($charSet.Lower | Get-Random -Count 6) + ($charSet.Number | Get-Random -Count 4) | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $ranijay86#$ -AsPlainText -Force
          New-LocalUser -Name "ranijay" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "ranijay"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "ranijay"
          # Masking the password in logs
          echo "::add-mask::$ranijay86#$"
          echo "RDP_PASS=$ranijay86#$" >> $env:GITHUB_ENV

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile "tailscale.msi"
          Start-Process msiexec.exe -ArgumentList "/i", "tailscale.msi", "/quiet", "/norestart" -Wait
          Start-Sleep -Seconds 15

      - name: Establish Tailscale Connection
        run: |
          $authKey = "${{ secrets.TAILSCALE_AUTH_KEY }}"
          $hostName = "ranijay-rdp-${{ github.run_id }}"
          & "C:\Program Files\Tailscale\tailscale.exe" up --authkey=$authKey --hostname=$hostName
          
          $tsIP = & "C:\Program Files\Tailscale\tailscale.exe" ip -4
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

      - name: Maintain Connection (6 Hours)
        run: |
          Write-Host "==============================="
          Write-Host "IP ADDRESS : $env:TAILSCALE_IP"
          Write-Host "USERNAME   : ranijay"
          Write-Host "PASSWORD   : $env:$ranijay86#$" 
          Write-Host "==============================="
          
          $endTime = (Get-Date).AddMinutes(350) # 10 mins buffer before runner kills itself
          while ((Get-Date) -lt $endTime) {
              Write-Host "[$(Get-Date)] Session Active..."
              Start-Sleep -Seconds 300
          }
